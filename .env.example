# --- Login automático (RECOMENDADO) ---
# Sin esto hay que re-sembrar la cookie a mano cada ~40 min, porque la sesión de
# Caravelo vence por tiempo absoluto y no hay keep-alive que la sostenga.
# Con esto el radar se re-loguea solo y te olvidás.
AYCF_EMAIL=
AYCF_PASSWORD=

# --- JetSmart / Caravelo (obligatorio) ---
# DevTools > Network > hacé una búsqueda en el portal > click en "availability/..."
# El UUID al final de esa URL:
AYCF_PASS_ID=
# Opcional si cargaste AYCF_EMAIL/AYCF_PASSWORD. Sirve para arrancar sin guardar
# credenciales, pero hay que renovarla a mano cada ~40 min. Es una credencial.
AYCF_COOKIE=

# --- Telegram (opcional, pero es la salida principal) ---
TELEGRAM_TOKEN=
# Tu chat: es el "dueño" del deploy, el único que hereda lo de estas env vars.
TELEGRAM_CHAT_ID=
# El secreto del webhook. Sin esto la URL del bot queda abierta.
TELEGRAM_WEBHOOK_SECRET=
# Bot privado: solo estos chats pueden hablarle (separados por coma).
# Vacío = cualquiera puede usarlo con SU propia cuenta de JetSmart.
TELEGRAM_ALLOWED_CHATS=

# --- Multi-usuario (obligatorio si otros van a conectar su cuenta) ---
# Clave para cifrar las contraseñas que la gente manda con /conectar.
# Sin esto el bot se niega a guardarlas. Inventala larga y no la rotes:
# si cambia, las credenciales guardadas dejan de poder descifrarse.
SECRET_KEY=

# --- Webhook genérico (opcional): Discord, Slack, ntfy, lo que uses ---
WEBHOOK_URL=

# --- Estado (sesión + dedupe) ---
# En Vercel hace falta Redis: el filesystem es efímero y la sesión se perdería.
# Sirve Upstash o Vercel KV (también lee KV_REST_API_URL / KV_REST_API_TOKEN).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Self-hosteando alcanza un archivo (default: .state.json)
STATE_FILE=

# --- Watches ---
# En Vercel, el JSON entero acá. Self-hosteando, usá watches.json.
WATCHES=

# --- Producción ---
# Inventalo largo. Vercel lo manda como Bearer; sin esto el endpoint queda público.
CRON_SECRET=
